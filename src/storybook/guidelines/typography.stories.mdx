import { Meta, Story, Canvas, Typeset } from '@storybook/addon-docs'
import { defaultTheme } from '../../theme/defaultTheme'

<Meta title="Guidelines/Typography" />

export const typography = {
  type: {
    primary: '"Mindset-Slim"',
    secondary: '"Mindset"'
  },
  weight: {
    regular: '400'
  }
}

# Typography

**Font:** Mindset-Slim

**Weights:** 400 (regular)

## Font sizes

<Typeset
  sampleText="Munchies Money"
  fontFamily={typography.type.primary}
  fontSizes={[
    defaultTheme.fonts.sizes.xxxs,
    defaultTheme.fonts.sizes.xxs,
    defaultTheme.fonts.sizes.xs,
    defaultTheme.fonts.sizes.sm,
    defaultTheme.fonts.sizes.md,
    defaultTheme.fonts.sizes.lg,
    defaultTheme.fonts.sizes.xl,
    defaultTheme.fonts.sizes.xxl,
    defaultTheme.fonts.sizes.xxxl
  ]}
  fontWeight={400}
/>

**Font:** Mindset

**Weights:** 400 (regular)

## Font sizes

<Typeset
  sampleText="Munchies Money"
  fontFamily={typography.type.secondary}
  fontSizes={[
    defaultTheme.fonts.sizes.xxxs,
    defaultTheme.fonts.sizes.xxs,
    defaultTheme.fonts.sizes.xs,
    defaultTheme.fonts.sizes.sm,
    defaultTheme.fonts.sizes.md,
    defaultTheme.fonts.sizes.lg,
    defaultTheme.fonts.sizes.xl,
    defaultTheme.fonts.sizes.xxl,
    defaultTheme.fonts.sizes.xxxl
  ]}
  fontWeight={400}
/>
